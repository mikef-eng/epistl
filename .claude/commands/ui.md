---
description: Fast, no-ceremony frontend styling tweak — hot-reload only, no tests/issues/CI. Usage: /ui <styling request>
---

Delegate to the `ui` subagent with this request:

$ARGUMENTS

Follow `.claude/agents/ui.md` exactly: presentation-layer files only, direct inline edit on the current branch, no tests/commits/issue changes, reply in one short sentence naming the file(s) touched.
